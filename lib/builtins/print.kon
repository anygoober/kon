from core.cffi import CStr

extern "C" {
    #include <stdio.h>
    fn printf => c_printf(format: *const char, ...) -> any;
}

pub fn println(content: string) {
    // TODO: use printf
    //       for future versions, avoid printf
}
